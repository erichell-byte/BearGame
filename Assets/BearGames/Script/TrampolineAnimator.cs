using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrampolineAnimator : MonoBehaviour
{
    /*
    [SerializeField] private Animator _animator;
    [SerializeField] private Transform _bear;

    private void OnTriggerEnter(Collider other)
    {
        if (other.name == "MedvedCar")
        {
            _animator.SetBool("isTrampoline", true);
            _animator.Play("Trampoline");
        }
    }

    private void Update()
    {
        //Debug.Log(_bear.transform.position.y);
        if (_bear.transform.position.y <= 0.65f)
        {
            _animator.SetBool("isTrampoline", false);
            //_animator.SetFloat("ReturnToRiding", 1f);
            _animator.Play("Riding");
        }
    }
    */
}
